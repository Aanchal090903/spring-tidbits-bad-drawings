{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "public" }
    }
  ],
  "installCommand": "npm install",  # Not needed, but harmless
  "buildCommand": "hugo --gc --minify"
}
